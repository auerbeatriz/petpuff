import { PedidoRepository } from "../repositories/PedidoRepository";

export class PedidoService {
/*
    async getPedidos(clienteId: number) {
        const pedidos = await PedidoRepository.getPedidos(clienteId)
        const response = pedidos.map(pedido => {
            return {
                ...pedidos,
                pelucia: {
                    ...orcamento.pelucia,
                    fotos: orcamento.pelucia.fotos.map(({ url }) => url)
                }
            }
        })

        return response
    }
        */
}